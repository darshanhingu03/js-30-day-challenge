// task:1 - Declare a variable using let, assign an initial value,reassign a new value, and log the value to the console.

let reassign_value = 1;

console.log("reassign_value: ", reassign_value);

reassign_value = 2;

console.log("task-1: ", reassign_value);
